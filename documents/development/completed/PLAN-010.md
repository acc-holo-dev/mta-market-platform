# PLAN-010 — Creator Analytics Foundation

> Этот файл объединяет СПЕЦИФИКАЦИЮ плана (§0–§15) и ЗАПИСЬ О ВЫПОЛНЕНИИ
> (EXECUTION RECORD, в конце документа) — по конвенции PLAN-002..009.
> Статус: **IMPLEMENTATION COMPLETE** (2026-09-11); production-верификация
> остаётся отдельным шагом (см. DEVELOPMENT/CURRENT.md).
> Обоснование выбора фазы: [NEXT-PHASE.md](../NEXT-PHASE.md) (§11, Цикл 5).

---

PLAN-010 — Creator Analytics Foundation

Репозитории:

- acc-holo-dev/mta-market-site (основной объём работ)
- acc-holo-dev/mta-market-document (этот план)
- acc-holo-dev/mta-market-module — НЕ затрагивается

Текущий фундамент проекта:

PLAN-001..009 ✅ (см. DEVELOPMENT/COMPLETED/)

Фундаментальные документы:

VISION.md
PRODUCT-ARCHITECTURE.md
PRODUCT-MODEL.md
PRODUCT-SURFACE-MAP.md
DAILY-EXPERIENCE.md

=================================================
0. ЦЕЛЬ PLAN-010
   ==================================================

Переход состояния:

ИЗ:

Создатель видит единственный сигнал о спросе — завершённые продажи.
Просмотры карточек не измеряются вообще: IDEAS §4 («Seller analytics уровня
ecommerce: просмотры → конверсия») пуст, CREATOR Loop (§9: Sales → Update →
Return) не даёт создателю понять, есть ли интерес до покупки.

В:

Каждое открытие страницы ресурса считается (честная page-view метрика);
создатель видит в своём магазине: просмотры за 30 дней (всего и по
ресурсам), конверсию просмотр→покупка. Только свои данные; без идентичностей
зрителей (агрегат-таблица, публичных метрик просмотров нет).

После PLAN-010 создатель отвечает на вопрос:

"Мой ресурс смотрят? Сколько смотревших покупают?"

— и принимает решения (обложка, описание, цена) на реальных данных.

=================================================
1. СТРАТЕГИЧЕСКАЯ ИДЕЯ
   ==================================================

План не называется:

"Analytics platform".

Суть — дать создателю минимальный честный сигнал спроса:

"Просмотры — это интерес. Покупки — это доверие. Конверсия их связывает."

Никаких сессий, воронок, гео, A/B — одна метрика (просмотры карточек) и
одна производная (конверсия). Всё остальное — будущие фазы из IDEAS §4.

=================================================
2. ПРИНЦИПЫ
   ==================================================

1. **Privacy**: не хранить идентичности зрителей. Только агрегат
   (ресурс × день × счётчик). Зрители — не пользователи в этой модели.
2. **Honesty** (Vision Principle 5): views = открытия страницы ресурса
   (page views), никаких inflate-хаков и никакого «примерного» подсчёта.
   Показывать продавцу именно открытия.
3. **Public metrics unchanged**: просмотры НЕ показываются покупателям
   (VISION §20: публичные агрегаты — только те, что разрешает бизнес-логика;
   публичность просмотров — решение будущей фазы).
4. **Real data only**: счётчик растёт только от реальных открытий страницы.

=================================================
3. WORKSTREAM A — МОДЕЛЬ
   ======================

A-001 — ResourceViewDaily

- resourceId (FK Resource, Cascade);
- day (дата, YYYY-MM-DD, MSK/UTC — UTC);
- views (Int);
- unique [resourceId, day]; индекс по resourceId.
Upsert-инкремент при открытии страницы. Никаких идентичностей, сессий,
cookie — это принципиальное решение (анонимно и дёшево).

---

A-002 — Границы

- Открытие страницы ресурса = GET /resources/:slug из web → отдельный
  публичный POST /resources/:slug/view с фронтенда (once per mount);
  GET остаётся чистым read (никаких side effects на чтение).
- Rate limit стандартный; валидация PUBLISHED.

=================================================
4. WORKSTREAM B — API
   ===================

B-001 — POST /resources/:slug/view

- публичный (гость считается так же);
- 404 для не-PUBLISHED;
- upsert инкремента (resourceId × today);
- без тела, без параметров, идемпотентность не требуется (честные page
  views: каждый mount = просмотр).

---

B-002 — Аналитика продавца

GET /seller/analytics (auth, seller):

- days=30 (фиксированный горизонт, константа);
- totalViews (сумма по всем своим ресурсам за 30д);
- byResource: [{ resourceId, slug, title, views30d, purchases30d,
  conversionPct }] — конверсия = завершённые покупки ресурса за 30д /
  просмотры × 100 (округление до целых; views=0 → conversion null);
- только свои ресурсы (PUBLISHED + все статусы? — аналитика по всем своим,
  включая черновики? Просмотры бывают только у PUBLISHED; по черновикам
  строки не показывать).

=================================================
5. WORKSTREAM C — ПОВЕРХНОСТИ
   ===========================

C-001 — Fire-and-forget просмотр

Страница ресурса: useEffect (once per mount) → POST /resources/:slug/view
(только если resource PUBLISHED). Не блокирует рендер, ошибки молча.

---

C-002 — Магазин: вкладка/блок «Аналитика»

/store (или /seller — где живёт кабинет): блок «Аналитика (30 дней)»:

- всего просмотров;
- конверсия средняя;
- таблица по ресурсам: просмотры / покупки / конверсия.

Empty state: «Пока нет просмотров».

=================================================
6. WORKSTREAM D — PERFORMANCE
   ===========================

- upsert по unique-индексу (одна запись на ресурс×день) — O(1);
- аналитика — один bounded-запрос агрегата по своим ресурсам;
- публичные поверхности не изменены (Home/activity не трогаем).

=================================================
7. WORKSTREAM E — SEED
   ====================

seed-plan005: заполнить ResourceViewDaily за последние 30 дней
(правдоподобные объёмы для demo-ресурсов, нули для новых) — чтобы блок
аналитики был живым сразу.

=================================================
8. TESTING — API
   ==============

- view: 201/200 для PUBLISHED; 404 для DRAFT/SUSPENDED; счётчик растёт
  ровно на 1 за вызов; отдельные дни дают отдельные строки;
- analytics: только свои ресурсы; чужие не видны; конверсия считается
  (создать покупку → проверить); views=0 → conversion null;
- без auth → 401; не-продавец → пустой набор (не 403? — решить: пусто).

=================================================
9. TESTING — PRIVACY
   ==================

- в payload аналитики нет идентичностей зрителей (их и не существует);
- просмотры не видны покупателям/гостям (карточка ресурса не содержит
  views — проверка JSON).

=================================================
10. TESTING — E2E (browser)
   =========================

- открыть страницу ресурса → в магазине продавца блок «Аналитика» показывает
  ≥1 просмотр этого ресурса (после refetch);
- mobile smoke /store.

=================================================
11. TESTING — REGRESSION
   ======================

Полный backend suite (372 + новые), Playwright 56 + новые. Особое внимание:
страница ресурса (PLAN-001/003), магазин продавца, activity (PLAN-006).

=================================================
12. ACCEPTANCE CRITERIA
   ====================

- [ ] ResourceViewDaily + миграция формальным путём, пакет в git.
- [ ] POST /resources/:slug/view работает (PUBLISHED only, честный счётчик).
- [ ] GET /seller/analytics: totalViews, byResource с конверсией.
- [ ] Блок «Аналитика» в кабинете продавца с empty state.
- [ ] Просмотры не публичны (только продавцу).
- [ ] Seed с историей просмотров.
- [ ] Backend suite зелёный (372 + новые); E2E 56 + новые; build ok; tsc
      чист; публичные поверхности (Home/activity/поиск) не изменены.
- [ ] §15 walkthrough пройден в браузере.

=================================================
13. FINAL WALKTHROUGH
   ===================

SCENARIO 1 — СОЗДАТЕЛЬ: публикует ресурс → читатель открывает карточку →
в магазине появляется «Просмотры: 1 (30 дней)». ✅ цель

SCENARIO 2 — СОЗДАТЕЛЬ: видит конверсию (просмотры vs покупки) по каждому
ресурсу → понимает, где теряется интерес. ✅ цель

=================================================
14. НЕ ДЕЛАТЬ
   ==========

- Идентичности зрителей, сессии, cookie-трекинг, гео;
- публичные показы просмотров (решение будущей фазы);
- воронки/A/B/heatmaps/источники трафика (IDEAS §4 остаётся пулом идей);
- analytics для услуг/серверов (только ресурсы);
- изменения Home/activity/поиска.

=================================================
15. FINAL PRINCIPLE
   =================

Создатель без данных о спросе — работает вслепую.

Одна честная метрика (просмотры) и одна производная (конверсия) дают
создателю достаточно, чтобы принимать решения, — и ничего больше, чем
платформа имеет право знать.

---

# EXECUTION RECORD — выполнено 2026-09-11

**Статус: IMPLEMENTATION COMPLETE** (2026-09-11)

## Итог

Цель плана достигнута: у создателя впервые появился честный сигнал спроса.
Каждое открытие страницы ресурса считается (page-view, без идентичностей
зрителей); в кабинете продавца — блок «Аналитика»: просмотры за 30 дней
(всего и по ресурсам), покупки за 30 дней, конверсия просмотр→покупка.
Просмотры — приватные данные продавца (не публичная метрика); публичные
поверхности (Home/activity/поиск/карточка ресурса) не изменены.

## Что сделано (по workstreams)

### WORKSTREAM A — модель и миграция
- `ResourceViewDaily` (resourceId + day unique, views counter, индексы).
  Зрительские идентичности не существуют по дизайну (§2.1) — агрегат
  ресурс × день × счётчик.
- Миграция формальным путём: 4 ops,
  `migrations/app/20260911T0343_plan010_creator_analytics` в git.

### WORKSTREAM B — API
- `POST /resources/:slug/view` (public; PUBLISHED only; upsert-инкремент
  resource × today; GET остался чистым read — side effect вынесен в
  отдельный POST, §A-002).
- `GET /seller/analytics` (auth, seller): days=30 фикс; totalViews /
  totalPurchases; byResource [{views30d, purchases30d, conversionPct}]
  (conversion = завершённые покупки 30д / просмотры, views=0 → null,
  views>0 без покупок → честные 0%); только свои ресурсы.

### WORKSTREAM C — поверхности
- Страница ресурса: fire-and-forget `countResourceView` once per mount
  (useRef guard против React double-effect), ошибки молча.
- Кабинет продавца: карточка «Аналитика» — итоги (просмотры/покупки/средняя
  конверсия) + таблица по ресурсам с deep links; empty state «Пока нечего
  анализировать».

### WORKSTREAM E — seed
- seed-plan005: ResourceViewDaily за 30 дней для PUBLISHED-ресурсов
  (волна интереса: новее — больше; идемпотентно по unique-паре).

### §8–§11 — тесты и регресс
- `tests/plan010-analytics.test.ts` (6): счётчик (инкремент, отдельные
  дни), 404 для draft, аналитика (только свои, конверсия 1/7→14%, чужие
  невидимы, 0 покупок → честные 0%), auth 401, приватность (payload
  страницы ресурса без views).
- Полный backend: **378/378** (372 + 6).
- Playwright: **59/59** (56 + 3: открытие ресурса регистрирует просмотр
  (before/after по API аналитики); блок «Аналитика» в кабинете; mobile
  читаемость — login на десктоп-вьюпорте, затем resize (кнопка «Выход»
  живёт в mobile drawer — урок PLAN-008/009)).
- Production build web: exit 0; tsc чист у server и web.

### Финальные счёта приёмки (2026-09-11)

- Backend (vitest): **378/378** (337 → 349 → 358 → 365 → 372 → 378).
- Playwright browser E2E: **59/59** (37 → 42 → 47 → 52 → 56 → 59).
- Миграция: 4 ops (`20260911T0343_plan010_creator_analytics`).
- Performance: view upsert O(1) по unique-индексу; аналитика — один
  bounded-запрос; публичные поверхности не изменены.

## Уроки окружения (дополнение)

1. **Mobile uiLogin**: кнопка «Выход» живёт в mobile drawer — для
   mobile-ассертов логиниться на десктоп-вьюпорте, затем setViewportSize.
2. **Honest metrics**: page-view = каждое открытие страницы; React
   StrictMode double-effect гасится useRef-guard'ом (иначе dev-данные
   надуваются ×2).

## §13 Walkthrough

- SCENARIO 1 (СОЗДАТЕЛЬ): читатель открывает карточку → в «Аналитике»
  появляется просмотр. ✅ (E2E 1)
- SCENARIO 2 (СОЗДАТЕЛЬ): конверсия по каждому ресурсу (таблица). ✅ (E2E 2)

## Ограничения

- Просмотры — page-opens: refresh считается (стандартная семантика page
  views); отдельные зрители не различаются (принцип privacy §2.1).
- Просмотры не публичны (показ «N просмотров» на карточке — решение
  будущей фазы, VISION §20).
- Analytics только для ресурсов (услуги/серверы — будущие фазы).
- production-верификация остаётся решением владельца инфраструктуры.
