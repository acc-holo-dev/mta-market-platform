# REFERENCE — замороженные технические контракты

Это **не** статусная документация и не энциклопедия. Здесь лежат только
межрепозиторные контракты, изменение которых требует согласованного релиза
(см. матрицу совместимости):

| Файл | Что фиксирует |
|---|---|
| [drm-protocol-v2.md](old_drm-protocol-v2.md) | DRM Protocol v2: константы, wire-формат, подписи. Реализован в `site/server/src/lib/drm/protocol.ts` и `module/src/drm/**` (пути монорепо PLAN-011; при расхождении кода и документа код выигрывает). |
| [repository-contract.md](old_repository-contract.md) | Что владеет/не владеет каждый из трёх репозиториев; правила независимых релизов. |
| [compatibility-matrix.md](old_compatibility-matrix.md) | Проверенные сочетания версий site ↔ REST ↔ DRM protocol ↔ module. |

**При расхождении кода и документа код выигрывает** — документ корректируется.
Изменение любого замороженного значения требует bump версии протокола (v3),
а не редактирования на месте.
