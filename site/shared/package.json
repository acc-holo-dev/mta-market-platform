{
  "name": "@mta-market/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared DTO types, error-envelope helpers and domain labels used by site components (PLAN-010 Rule 006: cross-component coupling goes through shared types and contracts/, never through source imports).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo 'shared: no lint config yet' && exit 0"
  },
  "devDependencies": {
    "@mta-market/tsconfig": "workspace:*",
    "typescript": "^6.0.3"
  }
}
