# Development environment — native module (module/).
# Identity source of truth: module/config/module.toml (name/title/author/version).
module:
  name: base
  title: Base Module
  version: 2.1.0
build:
  preset: linux-gcc
  cxxStandard: 20
  unity: true
  lto: true
drm:
  marketApiBase: http://localhost:3001
  keyStoreHome: ~/.mta-market
