# Verified compatibility — module component
version: 1
last_verified: 2026-09-11
rows:
  - module: base 2.1.0 (module/config/module.toml), SDK_VERSION 1.0.0
    platform: Linux x64 (GCC 15, OpenSSL 3.5) — ctest 3/3 + DRM make-tests PASS
    integration: market_client -> /integration/heartbeat|review-tokens (PLAN-005)
  - windows: best-effort CI; verified status NOT CLAIMED (see CURRENT.md blockers)
notes:
  - vendored Lua headers must stay byte-identical between module/third_party/mta-sdk/lua and module/third_party/lua/src (CI cmp gate)
  - module identity flows from module/config/module.toml only
